# CC Java SDK

this is the SDK implementation for authoring java cloud compute plugins

building:

gradle build -x test: builds the distribution without dependencies
gradle shadow: builds the distribution with dependencies